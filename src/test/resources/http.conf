error_log  /tmp/error.log;
pid        /tmp/nginx.pid;
events {
  worker_connections  512;
}

http {

  server {
    listen    3000;
    include   /tmp/server.location;
  }

  include   /tmp/nacos.upstream;
}
